<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.ag.push">

    <application>

        <!-- 配置第三方Receiver -->
        <receiver
            android:name=".GetuiPushReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.igexin.sdk.action.${GETUI_APP_ID}" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
